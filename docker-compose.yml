services:
  api:
    build: .
    container_name: agentic-rag-intelligence-platform-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    command: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000
